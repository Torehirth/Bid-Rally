/**
 * Navigates to the specified URL by setting the window location href
 * @param {string} href - The URL or path to navigate to
 */
export const navigateTo = (href) => {
  window.location.href = href;
};
