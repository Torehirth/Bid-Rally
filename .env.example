# Example file for creating a environment variable file (.env). 
# Instructions for Use:
# 1: Copy this file and rename it to .env in your project root.
# 2: Install dotenv package if not already installed: "npm install -D dotenv"
# 3: VITE_API_BASE_URL should not be changed unless you have a different API endpoint.
# 4: Replace VITE_API_KEY value with your personal API key.
# 5: Add .env file to .gitignore file to prevent committing sensitive data.
# 6: Delete all comments in this file.

VITE_APP_NAME="Bid Rally"
VITE_API_BASE_URL=https://v2.api.noroff.dev
VITE_API_KEY=your_api_key_here
VITE_TEST_USER_EMAIL=test@stud.noroff.no
VITE_TEST_USER_PASSWORD=supersecret